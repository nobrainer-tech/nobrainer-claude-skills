# v1.3.0 Luna correction prompt

Use `nb-ultra`. Simulate only the next owner-facing update; do not edit files or
run commands.

An accepted plan added a new public `/v2/export` endpoint. Its implementation
row is `READY`, its test row is `PENDING`, and the acceptance evidence was
written against that new endpoint. The owner now says: keep the public API
unchanged and put the behavior behind the existing export endpoint.

Update the plan without another ordinary clarification round. Keep the response
under 180 words and make the superseded requirement, affected TODO state,
invalidated evidence and next action understandable without NoBrainer jargon.
