# v1.2.0 Dispatcher trigger-scope evaluation — frozen candidate prompt

You are evaluating skill discovery from the complete relevant frontmatter and
boundary contract below. Do not use tools, browse, read files, contact sessions
or implement anything. Treat each case independently.

For each case return exactly four lines:

```text
CASE <letter>
LOAD_DISPATCHER: YES | NO
PRIMARY_OWNER: <one owner>
RATIONALE: <one concise sentence grounded in the contract>
```

## Source binding

```text
ULTRA_SHA256: aa14995abe77e92d1b03851a05a9e857eeff0963e581b2d1731587a1f2489f3a
TEAM_SHA256: 7727cb86d435b3d755a9bc359d2347003cb57c5582bbcace7ce45e87ec4d541c
DISPATCHER_SHA256: a64f7de5d852b54a82ba24dd2344318d9f70b98842c689e6f838bd9a792e19d5
SESSIONS_SHA256: 8b9a78ee578901e7bc74db3cd2040808a4d0368b40ddaf327d30ccdf79fd837f
FROZEN_UTC: 2026-08-28T20:29:54Z
```

## Available skill descriptions

`nobrainer-ultra`:

> Use when the owner says nb-ultra, nb-flow, nb-workflow, ultracode, or asks to
> set up, upgrade, or reconcile a project's agent workflow; or wants one
> non-trivial task taken from brief requirements through a complete skill-routed
> plan, guarded autonomous execution, useful multi-session work and final
> evidence audit.

`nobrainer-team`:

> Use when the owner says nb-team or nobrainer-skill-browser, asks to build a
> specialist agent team, or a non-trivial execution map needs
> installed-capability inventory, role selection, safe skills.sh lookup, or
> parallel workers without flooding the project with permanent skills or
> sessions.

`nobrainer-dispatcher`:

> Use when the owner says nb-dispatcher or nobrainer-dispatcher, or when an
> approved execution map has multiple queued work units that require ready-set
> selection, dependency-aware ordering, bounded parallel batches, retry
> scheduling, or routing audited results to the next queued unit; do not use for
> one coherent task, one bounded delegate, a standalone receive-audit,
> requirements, team design, or session transport.

`nobrainer-sessions`:

> Use when the owner says nb-sessions, nb-multi, or session-handoff, or asks for
> visible named reusable multi-session work with exact identity, isolated write
> scopes, audited handoffs, recovery, or controlled parallelism; do not use for
> a coherent one-session task.

## Boundary contract

- Ultra owns intent, the complete execution map and lifecycle.
- Team owns the minimum roles and capability sources.
- Dispatcher owns which already-defined work unit may run now.
- Sessions owns exact session identity, transport, lease and receive-audit.
- A single coherent work unit stays in MAIN and marks Dispatcher `NOT_NEEDED`.
- Dispatcher never invents requirements, designs the team, creates transport or
  accepts worker evidence.

## Independent cases

### Case A

The owner explicitly says: “Use nb-dispatcher to inspect this already-approved
map.” The map currently contains one coherent work unit and no queue.

### Case B

An approved execution map has `API-1`, `UI-1` and `E2E-1` queued. `E2E-1`
depends on the first two. The next safe ready set and bounded parallel batch
must be selected; roles and acceptance criteria are already defined.

### Case C

An approved four-unit queue has one blocked unit with remaining retry budget and
new logs that change its blocker fingerprint. Another unit was independently
accepted. The workflow must decide whether to schedule the retry or release a
different ready unit.

### Case D

There is exactly one bounded delegate. Its session identity and write scope are
already known, and it has returned one report. The only requested action is a
fresh receive-audit of that report and lease readback; there is no queued
successor.

### Case E

The owner gives a vague product goal and asks which specialist roles and skills
would be needed. There is no approved execution map, no task queue, no defined
dependencies and no acceptance criteria.
