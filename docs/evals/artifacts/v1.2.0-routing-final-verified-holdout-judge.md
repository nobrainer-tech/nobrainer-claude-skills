CASE_SCORES:
A: PASS — Superseded ready work is stopped, dependent work remains pending, old evidence is invalidated, and readiness requires a new fingerprint and gate recomputation.
B: PASS — Routing stops; Sessions owns controlled-stop readback, and no execution, cancellation, lease, or delivery state is invented.
C: FAIL — `CLIENT-8` is marked `PENDING`, but it must remain `BLOCKED` while `AUTHZ-8` is `CORRECTION_REQUIRED`.
D: PASS — `INDEX-5` remains `READY`; missing target/message-ID readback prevents `SENT`, with no blind retry.
E: PASS — Correctly returns `RESEARCH_BLOCKED` and selects no workaround or remedy.
HARD_FAILURES: NONE
MATERIAL_FINDINGS: C — `CLIENT-8` has the wrong required state.
VERDICT: FAIL — 1/5 cases
