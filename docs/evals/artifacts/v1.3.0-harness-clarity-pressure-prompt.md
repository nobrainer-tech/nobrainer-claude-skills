# v1.3.0 default Ultra pressure prompt

Use `nb-ultra`. Simulate the initial response and plan only; do not edit files
or run commands.

Task: Add resumable CSV import to an existing web application. It touches the
upload endpoint, background job and progress UI. Invalid rows must be reported,
retries must not duplicate accepted records, and the existing synchronous path
must remain compatible. The repository is clean. The implementation may take a
few hours but fits one session and has no production mutation in this task.

Show the owner enough information to understand the outcome, scope, progress,
proof and next action.
