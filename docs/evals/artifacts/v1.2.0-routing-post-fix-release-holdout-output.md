## Case A

TRANSITION: `OWNER_DECISION_CHANGED`; supersede the CSV requirement and plan `p-csv`, invalidate `EXPORT-1` and dependent `DOC-1` TODO/evidence, then rebuild the affected path.
OWNER_AND_METHOD: Ultra owns the canonical correction; do not implement or design JSON, dispatch, or claim a lease.
EVIDENCE_GATE: All evidence tied to CSV/`p-csv` is stale and cannot support readiness.
NEXT_ACTION: Record JSON as the new requirement and rebuild the plan with a new, currently unknown fingerprint.

## Case B

TRANSITION: Dispatcher commits `MIGRATE-2: READY -> SENT`.
OWNER_AND_METHOD: Dispatcher owns the ledger transition; Sessions owns the exact identity and single transport.
EVIDENCE_GATE: Fresh preflight passed and `msg-204` was read back from the exact target; lease claim and execution remain unproven.
NEXT_ACTION: Await the worker report, then run `RECEIVE_AUDIT` before `RECONCILE`.

## Case C

TRANSITION: Dispatcher marks `AUTH-2: CORRECTION_REQUIRED -> ACCEPTED` and releases `UI-2` to `READY`.
OWNER_AND_METHOD: Dispatcher owns `RECONCILE`; Build, independent review, and Sessions audit have completed their gates.
EVIDENCE_GATE: Fresh repaired-diff tests, review proof, complete receive-audit, exact binding, side effects, runtime, and `LEASE: RELEASED` all passed.
NEXT_ACTION: Leave `UI-2` ready; do not send the successor.

## Case D

TRANSITION: `PROBLEM_GATE -> RESEARCH_BLOCKED`; pause remediation.
OWNER_AND_METHOD: Ultra requires relevant wiki `GET` followed by current `nobrainer-research`; current research is unavailable.
EVIDENCE_GATE: Stale wiki guidance cannot resolve disagreement with current configuration/logs or establish provider behavior.
NEXT_ACTION: No retry value may be chosen; obtain current research/provider evidence before selecting a remedy.

## Case E

TRANSITION: `RECOVER` makes no transition; `SYNC-9` remains `BLOCKED`, attempt 1 of 3, fingerprint `remote-503`.
OWNER_AND_METHOD: Dispatcher owns `RECOVER`; do not contact Sessions or a worker.
EVIDENCE_GATE: Recovery requires new evidence or a changed condition, and neither exists.
NEXT_ACTION: Preserve the prior blocker and evidence; wait for a genuinely new fact.
