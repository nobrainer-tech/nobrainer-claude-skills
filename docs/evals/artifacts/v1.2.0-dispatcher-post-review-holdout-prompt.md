# Frozen dispatcher post-review holdout prompt — v1.2.0

Read only `skills/nobrainer-dispatcher/SKILL.md`. Do not inspect tests, release
notes or other evaluation artifacts. Do not edit files, contact real sessions or
perform implementation work.

Act as NoBrainer Dispatcher. Evaluate each independent case with the complete
closeout contract. Choose one safe transition, preserve unknown facts and state
only decision-relevant reasons.

## Case A — schedule before transport exists

Plan fingerprint `z-31` is current. Team design, outcomes, methods,
dependencies, acceptance checks, rollbacks and owner gates are complete. The
ledger begins with both tasks `PENDING`; budget is `max_active=2`.

| Task | Dependencies | Write scope | Checkout requirement | Mutable state |
|---|---|---|---|---|
| CORE-1 | none | `packages/core/` | isolated | core only |
| GUIDE-1 | none | `docs/guide/` | isolated | none |

No session, host, checkout identity, lease or active-turn readback exists yet.
Run `SCHEDULE`, not `DISPATCH`.

## Case B — successful transport readback

`CORE-1` is already `READY` and selected alone. Sessions now proves exact thread
and host identity, the expected isolated checkout, no active turn, lease
`UNCLAIMED`, and successful prompt transport with a message readback. There is no
evidence that the worker claimed the lease or started execution. Run `DISPATCH`.

## Case C — unresolved transport preflight

`GUIDE-1` is `READY`. Sessions cannot verify its checkout or lease owner and no
prompt was sent. Run `DISPATCH`.

## Case D — method-preserving correction

`FACT-1` used method `nobrainer-research` and is `REPORTED`. Sessions
`RECEIVE_AUDIT` verifies identity, checkout, released lease and evidence, but
finds one isolated incorrect source date. `FACT-1` has one blocked successor and
no owner gate. Run `RECONCILE`.
