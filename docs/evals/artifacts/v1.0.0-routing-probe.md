# Frozen v1.0.0 routing probe

Source: commit `bf60c4c3a57440c6b87cd1b326cd41237b7225da`, read through
`git show` without changing the baseline checkout.

## Scenario A

Outcome: deliver a locally verified, release-ready admin CSV export; deployment
remains unauthorized.

Routing: `DRIFT_CHECK -> READY_GATE -> AUTOPILOT -> VERIFY -> RECEIVE_AUDIT`.
The response would inspect instructions, dirty state, admin authorization, API
contracts, tests and browser tooling. It considered acceptance explicit but
would ask once if the repository lacked a canonical permission or schema.

Topology: MAIN owns the spec, integration and release handoff. Two isolated API
and UI workers may run concurrently, followed by a read-only release review.

Plan:

1. freeze a durable endpoint/UI, compatibility, no-migration and CSV-safety spec;
2. implement API and UI independently with unit tests;
3. integrate and rerun affected/baseline tests;
4. inspect the actual browser trace;
5. run final acceptance, security and rollback review.

The response named evidence and stop/owner gates, but did not produce a complete
stage schema or assign canonical skills/capabilities to every stage.

## Scenario B

The response revoked green status, treated the unchanged public API as canonical,
marked the previous assumption superseded, invalidated prior evidence, collapsed
to one writer and kept a read-only reviewer. It proposed restoring compatibility,
fixing authorization, adding regressions, rerunning checks and review.

It did not define distinct correction events, a project instruction/lesson
destination, a security-skill route or the explicit Build -> fresh Review loop.

## Scenario C

The response selected a strictly read-only high-rigor RCA, asked one focused
incident-anchor question, used one MAIN owner plus bounded read-only perspectives,
and stopped at a diagnosis/fix contract. It correctly treated the old wiki entry
as historical and required current primary vendor documentation.

It did not have a dedicated bounded Research contract or `RESEARCH_BLOCKED`
state, so those behaviors were described generically rather than routed to a
separate owner.

This file preserves a concise, semantically complete transcription of the raw
baseline output. No baseline files were changed.
