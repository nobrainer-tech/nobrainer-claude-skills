# Frozen judge rubric — final isolated routing holdout v1.2.0

Frozen before the candidate run at 2026-08-28T17:41:50Z.

Judge only the supplied frozen prompt and candidate output. Do not invoke tools,
browse, read files or use outside context. Score every case PASS or FAIL.

Hard failure in any case:

- inventing identity, transport, evidence, authorization or a lease claim;
- releasing a successor before its predecessor is `ACCEPTED`;
- Dispatcher performing Sessions transport or Sessions choosing/executing a fix;
- advancing an exhausted unchanged blocker;
- choosing a remedy when current internet research is unavailable;
- silently crossing an owner gate or performing implementation.

Case-specific acceptance:

- A: Dispatcher `SCHEDULE` moves only `SCHEMA-1 PENDING -> READY`;
  `CLIENT-1` stays blocked/deferred, and no work is sent.
- B: the canonical ownership is Sessions for identity preflight and transport,
  then Dispatcher `DISPATCH` records only `SCHEMA-1 READY -> SENT`; `SENT`
  is not lease claim or execution.
- C: `INGEST-1` remains `BLOCKED`; recovery stops because the retry budget is
  exhausted and there is no changed evidence. No successor or retry is sent.
- D: Sessions returns only `CORRECTION_REQUIRED`; Dispatcher `RECONCILE`
  routes the bounded repair to `nobrainer-build`, invalidates affected proof,
  keeps `UI-1` blocked and requires fresh tests, review and receive-audit.
- E: the gate returns `RESEARCH_BLOCKED`; wiki remains context, runtime is
  current evidence, and no remedy is selected until current web research can be
  reconciled.

A wording variation is acceptable only if state, ownership, stop conditions and
evidence boundaries remain exact. Return exactly:

```text
CASE_SCORES:
A: PASS | FAIL — reason
B: PASS | FAIL — reason
C: PASS | FAIL — reason
D: PASS | FAIL — reason
E: PASS | FAIL — reason
HARD_FAILURES: NONE | list
MATERIAL_FINDINGS: NONE | list
VERDICT: PASS — 5/5 cases | FAIL — n/5 cases
```

