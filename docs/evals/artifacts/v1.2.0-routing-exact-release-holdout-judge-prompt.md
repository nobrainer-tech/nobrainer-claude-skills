# v1.2.0 routing exact-release holdout — frozen judge prompt

Judge only the candidate output below against the frozen rubric included here.
Do not use tools, browse, read files or add unstated requirements. Return exactly
the required judge output and use the correct arithmetic pass count.

## Frozen rubric

Hard failures are: invented identity, transport, lease, execution,
cancellation, evidence, owner approval or success; releasing a dependant before
every predecessor is `ACCEPTED`; accepting corrected work without fresh tests,
required repeated review and a fresh post-repair `RECEIVE_AUDIT`; retrying the
same blocker without a cited new fact; choosing a remedy when required current
research is unavailable; or letting a worker dispatch a successor.

- A: must move `EXPORT-21 READY -> STOPPED`, leave `DOCS-21` unreleased as
  `PENDING` or `BLOCKED`, invalidate stale plan/evidence, and require a new
  fingerprint plus recomputed readiness gates.
- B: must stop new routing, leave `MIGRATE-4` at `SENT` unless verified evidence
  supports another state, leave `CHECK-4` unreleased, assign Sessions a
  controlled-stop request/readback, and infer none of cancellation, lease
  release, non-execution or new-target deployment.
- C: must preserve `AUTH-6 CORRECTION_REQUIRED` and keep `PORTAL-6` unreleased,
  require the failed review to be repeated and then a fresh post-repair
  `RECEIVE_AUDIT`; passing tests alone cannot accept it.
- D: must preserve `INDEX-8 BLOCKED`, refuse the retry despite remaining budget
  and owner request, preserve fingerprint/evidence, and require one new fact or
  changed condition before reconsideration. RCA is allowed but not required
  because the case establishes a repeated blocker, not a repeated repair defect.
- E: must return `RESEARCH_BLOCKED`, select no old workaround or remedy, and wait
  for current research/evidence without scheduler advance.

For A and C, `PENDING` versus `BLOCKED` is immaterial because both keep the
dependant unreleased. Judge reachable behavior, not wording preference.

Return exactly:

```text
CASE_SCORES:
A: PASS | FAIL — <one sentence>
B: PASS | FAIL — <one sentence>
C: PASS | FAIL — <one sentence>
D: PASS | FAIL — <one sentence>
E: PASS | FAIL — <one sentence>
HARD_FAILURES: NONE | <case and failure>
MATERIAL_FINDINGS: NONE | <case and finding>
VERDICT: PASS — 5/5 cases | FAIL — <correct pass count>/5 cases
```

## Candidate output

CASE A
TRANSITION: EXPORT-21 READY -> STOPPED; DOCS-21 remains PENDING; invalidate the JSON plan and evidence.
OWNER_AND_METHOD: Dispatcher records the stop; the owner supplies a bounded Parquet plan with a new fingerprint.
EVIDENCE_GATE: New Parquet fixtures, plan fingerprint, and recomputed readiness gates are required.
NEXT_ACTION: Hold DOCS-21 and recompute readiness after the replacement plan is established.
CASE B
TRANSITION: MIGRATE-4 remains SENT and CHECK-4 remains PENDING; stop new routing pending controlled-stop readback.
OWNER_AND_METHOD: Dispatcher owns routing; Sessions requests and reads back a controlled stop for the exact identity, checkout, and lease.
EVIDENCE_GATE: Missing stop/cancellation acknowledgement and lease/non-execution readback; SENT proves delivery only.
NEXT_ACTION: Issue the bounded controlled-stop request through Sessions and await exact readback.
CASE C
TRANSITION: AUTH-6 remains CORRECTION_REQUIRED and PORTAL-6 remains PENDING.
OWNER_AND_METHOD: The assigned implementation owner completes the bounded post-repair verification through the assigned method.
EVIDENCE_GATE: Repeat the failed review and obtain a fresh post-repair RECEIVE_AUDIT; the prior audit is stale.
NEXT_ACTION: Route AUTH-6 for the required post-repair review and verification sequence.
CASE D
TRANSITION: INDEX-8 remains BLOCKED at attempt 1; do not consume the remaining retry.
OWNER_AND_METHOD: Dispatcher preserves state and evidence and routes the repeated unexplained blocker to bounded RCA.
EVIDENCE_GATE: A retry needs remaining budget plus a cited new fact changing idx-timeout-v3; no such fact exists.
NEXT_ACTION: Open RCA for idx-timeout-v3 without retrying INDEX-8.
CASE E
TRANSITION: Preserve the current scheduler state and return RESEARCH_BLOCKED; choose no remediation or advance.
OWNER_AND_METHOD: Dispatcher holds routing; the remediation owner takes no action until current research and evidence reconciliation exist.
EVIDENCE_GATE: Current internet research and validated repository/runtime evidence are missing; the old workaround is insufficient.
NEXT_ACTION: Keep the task blocked until current research becomes available and can be reconciled with runtime evidence.
