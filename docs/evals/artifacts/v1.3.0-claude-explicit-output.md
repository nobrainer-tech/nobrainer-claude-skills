# Claude Code explicit candidate output

CLIENT: Claude Code `2.1.241`
MODEL: `claude-opus-5`
AUTH_READBACK: `loggedIn=true`, `authMethod=claude.ai`, `apiProvider=firstParty`
ULTRA_SHA256: `1d00852dd9222795a13ede2c53f524d2ce39d25ee542567485760c706b8139a0`
AUTOIMPROVE_SHA256: `85b700942b096cd40c111fe58b0293ef068d459ecd74c2443eb2d3ea9ab70a75`
INSTALL: isolated local plugin loaded with `--plugin-dir`
INVOCATION: explicit `/nobrainer-tech-skills:nobrainer-ultra`
TOOLS: `Read` only; plan permission mode; no Bash, edits or network
SESSION_ID: `33af34f3-85ae-4cd1-b6aa-ba774386557b`
PROMPT_SOURCE_SHA256: `10d593cee7c3ee81f52a5f8f2e179bbf0b39afe8295754e2881d4663b600babe`
SPECIALIST_READBACK: `PASS`, canonical Autoimprove SHA matched the value above
RAW_OUTPUT_SHA256: `f1123a29e3eccda186e2429fefc11b6dd0ea8e8d3e6852a458d376285498637f`
RAW_OUTPUT_WORDS: `345`
RESULT: `PASS_EXPLICIT`

The earlier unauthenticated probe is retained as historical blocker evidence.
After owner-driven OAuth, this clean explicit run loaded Ultra, followed its
routing contract and read the exact canonical Autoimprove body before planning.
Automatic description-based routing remains unverified.

## Stored owner-facing output

STORED_OUTPUT_NORMALIZATION: Markdown wrapping only
STORED_OUTPUT_SHA256: `551c6677ba489e46e592c39cbfcbfb5e18560d4e775b2f67abf53f6a7c625cc7`
STORED_OUTPUT_WORDS: `345`

Plan file writing is disabled in this session, so here is the owner-facing plan directly.

**Outcome:** a measured decision to promote one revised bug-triage prompt or retain the frozen baseline — evidence-bound claims, unknowns labeled, normally ≤120 words.

**Scope**

- Edit only an isolated copy of the prompt; baseline and its hash stay frozen.
- Non-goals: production, publishing, holdout disclosure or any tuning against the holdout.
- Budget honored as given: 2 rounds, ≤3 candidates per round, distinct hypotheses not paraphrases.

**Progress**

- [x] Objective, frozen baseline and budget are clear
- [>] Freezing gates and rubric before the first candidate
- [ ] Run rounds, then the single final holdout check
- Next: score the unchanged baseline on the three development cases only

**How it is measured and decided**

- Hard gates disqualify outright (not averaged): no root cause asserted beyond supplied evidence, every unsupported item explicitly labeled unknown, word budget respected, required triage sections present.
- Score survivors on the three development cases for evidence-binding, unknown labeling, concision and triage usefulness — median across independent judges, candidates anonymized and shuffled, judges not knowing the incumbent.
- Select a new champion only when it beats the current one beyond judge noise; on a near-tie prefer the shorter, simpler candidate. One optional graft may fold a loser's strongest section onto the champion, then rerun all gates.
- Stop on the first of: no meaningful gain, convergent candidates, hard-gate regression, budget exhausted, or a changed frozen input.
- `FINAL_HOLDOUT` runs sealed baseline and final champion once. Promotion requires all gates passing, gain confirmed on holdout, no protected regression, scoped diff and tested rollback. A gain that vanishes on holdout is overfitting: revert to baseline and record the negative result. Null result is a valid outcome.

**Report you get:** baseline→final development and holdout scores, compact round log, gate results, budget used, stop reason, diff and rollback, plus `PROMOTION`, `CHAMPION`, `HOLDOUT_RESULT`. Holdout content and per-case failure detail are never surfaced to generators or into the report.

**Gate:** replacing the live prompt remains yours to approve; nothing here executes.
