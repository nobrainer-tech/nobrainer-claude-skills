## Case A

TRANSITION: `SCHEMA-1 PENDING -> READY`; `CLIENT-1` remains `PENDING`.

OWNER_AND_METHOD: `nobrainer-dispatcher` — `SCHEDULE`.

EVIDENCE_GATE: `q-91` and all contracts pass; absent session/lease/transport evidence blocks dispatch only.

NEXT_ACTION: Use Sessions setup/delegate for `SCHEMA-1`; do not send work yet.

## Case B

TRANSITION: `SCHEMA-1 READY -> SENT`; highest justified state is `SENT`.

OWNER_AND_METHOD: Dispatcher `DISPATCH`; Sessions owns identity preflight and transport.

EVIDENCE_GATE: Exact identity, isolated checkout, free lease, inactive turn and message-ID readback prove delivery, not execution.

NEXT_ACTION: Await the worker report, then run `RECEIVE_AUDIT` and `RECONCILE`.

## Case C

TRANSITION: `INGEST-1` remains `BLOCKED`; no retry or `BLOCKED -> READY`.

OWNER_AND_METHOD: `nobrainer-dispatcher` — `RECOVER`.

EVIDENCE_GATE: Attempt budget is exhausted and `provider-429-us` plus byte-identical evidence provide no changed condition.

NEXT_ACTION: Stop blind retries; preserve the blocker and route the repeated fingerprint to RCA or await changed evidence.

## Case D

TRANSITION: Audit fails for authorization bypass; `API-1 REPORTED -> CORRECTION_REQUIRED`; `UI-1` remains `PENDING`.

OWNER_AND_METHOD: Dispatcher `RECONCILE` routes repair to `nobrainer-build`; Sessions owns the audit.

EVIDENCE_GATE: Identity, diff, checkout, lease and targeted tests pass, but the required review fails; affected evidence is invalidated.

NEXT_ACTION: Run one bounded build repair, then fresh tests and `RECEIVE_AUDIT` before releasing `UI-1`.

## Case E

TRANSITION: Ambiguous problem -> `RESEARCH_BLOCKED`; no remedy may be selected.

OWNER_AND_METHOD: Ultra `PROBLEM_GATE`; wiki context is followed by required `nobrainer-research`.

EVIDENCE_GATE: Historical three-retry guidance conflicts with current five-attempt runtime evidence, and current research is unavailable.

NEXT_ACTION: Preserve the ambiguity and stop until current authoritative evidence is available.
