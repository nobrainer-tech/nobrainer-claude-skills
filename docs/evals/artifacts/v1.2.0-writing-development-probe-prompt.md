# Frozen writing development probe — v1.2.0

Read only `skills/nobrainer-writing/SKILL.md`. Do not inspect tests, release
notes, research references or other evaluation artifacts. Do not edit files,
browse or invent missing facts.

Act as NoBrainer Writing. Treat every case as independent. Return exactly five
sections named `## Case A` through `## Case E`. Under each heading return only
the finished prose requested by that case, with no audit commentary.

## Case A — dense project status

Compress the source into a Slack status of at most 75 words. Preserve every
material name, number, date, command, caveat and next action.

Source:

> Hi everyone! I just wanted to take a moment to give you a quick update
> regarding the account migration. On 28 August 2026 we migrated 47 of 52
> accounts. Five remain blocked because SSO metadata from Acme is missing.
> `python3 scripts/verify_migration.py` passed for all 47 migrated accounts. We
> have not tested production login for the five blocked accounts. Anna needs to
> send the metadata by 14:00 CEST; after that Jan will rerun the verifier. Do not
> call the migration complete until all 52 production login checks pass.

## Case B — already concise

Rewrite only if doing so is materially clearer. Do not add detail or make the
message longer than necessary.

Source:

> PR #42 is blocked: the Linux job fails on Python 3.14 because `distutils` is
> unavailable. Keep the PR open; Marta will replace the dependency and rerun CI.

## Case C — humanize without fabrication

Rewrite the source as one concrete website paragraph. The only supported proof
is the two supplied measurements. There is no voice sample, customer quote or
evidence that the product caused a business outcome.

Source:

> Our revolutionary platform seamlessly unlocks transformative efficiency and
> empowers forward-thinking finance teams to supercharge their journey. It
> processed 12,480 invoices in July, with a median review time of 3m12s.

## Case D — safety caveat

Write a help-center note of at most 50 words. Preserve encryption, retention,
the legal-hold exception, the fact that deletion is not immediate and the admin
action.

Source:

> Backups are encrypted at rest. They are retained for 30 days. Deleting a
> workspace starts deletion, but legal holds may delay final removal. This is
> not immediate, and admins should export required records first.

## Case E — useful code comment

Replace the existing comment with one concise comment. Explain the non-obvious
policy, not the visible conditional.

Code:

```python
# Here we check if attempts is greater than or equal to 3. If it is, we return
# STOPPED. This is an important step that helps ensure the process doesn't keep
# going forever.
if attempts >= 3:
    return STOPPED
```

Known rationale: the third retry is the externally agreed retry budget; any
further retry requires new evidence.
