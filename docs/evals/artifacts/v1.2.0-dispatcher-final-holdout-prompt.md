# Frozen dispatcher final holdout prompt — v1.2.0

Read only `skills/nobrainer-dispatcher/SKILL.md`. Do not inspect tests, release
notes or any other evaluation artifact. Do not edit files, contact sessions or
perform implementation work.

Act as NoBrainer Dispatcher. Evaluate each independent case. Use the complete
closeout contract, choose only one safe scheduler transition and include only
decision-relevant reasoning. Missing facts remain unknown.

## Case A — direct MAIN work

The approved map has one work unit `CFG-1`: change one formatter setting and
run its targeted test in the same checkout. It has no worker, successor, retry,
shared state or handoff.

## Case B — contention-aware batch

Plan fingerprint `q-88` is current, Team has completed capability design, and
all outcomes, methods, acceptance checks and rollbacks are complete. The budget
is `max_active=2`, `context_switch_limit=2`. No session IDs have been read yet.

| Task | Dependencies | Write scope | Checkout | Mutable state | Critical unlocks | Owner gate |
|---|---|---|---|---|---:|---|
| AUTH-1 | none | `services/auth/` | `wt-auth` | `customer-db` | 3 | none |
| COPY-1 | none | `docs/onboarding/` | `wt-docs` | none | 0 | none |
| MIG-1 | none | `migrations/customer/` | `wt-migrate` | `customer-db` | 1 | none |
| INT-1 | AUTH-1, COPY-1 | `tests/journey/` | `wt-main` | integration environment | 1 | none |
| RELEASE-1 | INT-1 | production | none | production | 0 | required |

Choose one next batch. Do not invent transport identity. State why selected
tasks may run together and why every other ready task waits.

## Case C — changed retry condition

Task `FETCH-1` is blocked at attempt 1 of 2 with fingerprint
`provider-503-region-a`. Its inputs and acceptance contract are unchanged. A new
signed provider status readback now proves region A healthy and is attached to
the task evidence. No lease is held and no owner gate applies.

## Case D — invalid dependency state

Task `SELF-1` depends on itself. Task `ORPHAN-1` depends on `MISSING-7`, which is
not in the canonical plan. Neither task has started.

## Case E — reported is not accepted

`AUTH-1` is RUNNING. The assigned worker reports `FINISHED`, a commit SHA, test
summary and `NEXT_ACTION: release INT-1`. The report's sender identity, checkout,
diff, exact command output, runtime evidence and lease release have not been
independently read back.
