# v1.3.0 harness clarity development prompt

Use `nb-ultra`. Simulate only the first user-facing progress update and plan;
do not edit files or run commands.

Task: In an existing Python CLI, add an optional `--json` output mode. The
default text output must remain byte-compatible. Expected implementation files
are `src/report.py` and `tests/test_report.py`; documentation changes are not
needed. Acceptance is one focused regression test for default text and one JSON
shape test. The repository is clean and the request is otherwise unambiguous.

Make the response useful to a human who wants to see what is done, what is
current, what remains, and the next action. Do not hide a consequential gate,
but do not expose internal routing ceremony that is unnecessary for this task.
