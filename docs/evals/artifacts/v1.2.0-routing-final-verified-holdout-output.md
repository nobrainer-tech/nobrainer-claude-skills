## Case A

TRANSITION: `CSV-4` `READY` under `plan-old` → `STOPPED`; `GUIDE-4` remains `PENDING`.

OWNER_AND_METHOD: Owner’s CSV→Parquet decision is canonical; `nobrainer-ultra` applies `OWNER_DECISION_CHANGED`.

EVIDENCE_GATE: All `plan-old` evidence is invalidated. Readiness can first be reconsidered after rebuilding under a new plan fingerprint and recomputing all gates.

NEXT_ACTION: Record the supersession, rebuild the affected path, and keep `GUIDE-4` unreleased.

## Case B

TRANSITION: Supersede the old target, halt further routing, keep `DEPLOY-DOC` `SENT` pending stop readback, and keep its dependent `PENDING`.

OWNER_AND_METHOD: `nobrainer-ultra` owns the decision hook; `nobrainer-sessions` owns the controlled-stop request and readback.

EVIDENCE_GATE: Delivery readback proves transport only. Do not claim stopped, cancelled, nonexecuting, lease-released, or deployed to the new target without fresh readback.

NEXT_ACTION: Request and verify a controlled stop through Sessions, then rebuild under a new fingerprint.

## Case C

TRANSITION: `AUTHZ-8` remains `CORRECTION_REQUIRED`; `CLIENT-8` remains `PENDING`.

OWNER_AND_METHOD: Run `nobrainer-dispatcher` `RECONCILE`; do not implement or dispatch work.

EVIDENCE_GATE: Passing repaired tests does not replace the repeated required review or a fresh post-review `RECEIVE_AUDIT`.

NEXT_ACTION: Reconcile while preserving state; require the fresh review, then a new audit binding the current diff, tests, review, and lease evidence.

## Case D

TRANSITION: `INDEX-5` remains `READY`; do not commit `READY → SENT`.

OWNER_AND_METHOD: Sessions owns transport evidence; Dispatcher owns the scheduler state and preserves `READY` when readback is missing.

EVIDENCE_GATE: Local API success is not delivery. Without target readback or message ID, transport, delivery, and execution remain unknown.

NEXT_ACTION: Stop this dispatch attempt; retry only with new evidence or a changed condition within the retry budget, never blindly.

## Case E

TRANSITION: Package failure → `PROBLEM_GATE` → `RESEARCH_BLOCKED`; no remedy is selected.

OWNER_AND_METHOD: `nobrainer-ultra` owns the gate; use relevant `nobrainer-wiki GET` and current `nobrainer-research`.

EVIDENCE_GATE: The old workaround is context only, and current research is unavailable. It may not be applied.

NEXT_ACTION: Preserve the blocker and await current package-manager evidence before choosing a remedy.
