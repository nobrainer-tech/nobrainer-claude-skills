# Codex baseline pressure output

CLIENT: Codex CLI `0.149.1`
MODEL: `gpt-5.6-sol`
REASONING_EFFORT: `none`
SESSION_ID: `01a0542b-7e15-77d2-be25-683f339b0b4d`
ULTRA_SHA256: `65f54bcb254ade94e759ed12f069602d62ecc03b4540e7d7d9d5a06152e7a909`
PROMPT_SHA256: `a795e5b9531c4606f40407c7ea6e7663758bc56760e8a0df7a86a3f746cd0345`
OUTPUT_SHA256: `bc7ca90414e5a05715e97842542970953c0396083f18c44a3928a12913b97bee`
OUTPUT_WORDS: `547`
RESULT: `FAIL`

The alias-only prompt did not establish an exact skill load. The output is
preserved because it establishes the user-facing RED: it invents an unseen
implementation and presents a long design walkthrough before repository
inspection.

## Output

### Simulated initial response

I’ll add resumable, idempotent CSV imports across the upload endpoint, background processing, and progress UI while preserving the current synchronous contract.

The intended outcome:

- Valid rows are accepted exactly once, including after retries or worker restarts.
- Invalid rows are retained with row number and actionable validation errors.
- Imports resume from durable checkpoints.
- The UI shows processed, accepted, rejected, and remaining counts.
- Existing synchronous callers continue working unchanged.
- No production systems or data are touched.

Current progress: planning only. The repository is clean but uninspected, and no commands or file changes have been made.

### Proposed implementation plan

1. **Map the existing flow**
   - Inspect the upload endpoint, CSV parser, persistence layer, job framework, and progress UI.
   - Record the current synchronous request/response contract and tests.
   - Identify existing uniqueness and transaction guarantees.

2. **Define the import state model**
   - Add a durable import record with status, total/processed/accepted/rejected counts, checkpoint, and timestamps.
   - Store invalid-row reports separately or as a downloadable artifact.
   - Use stable row identities derived from the import ID and source row number, optionally reinforced by a business-key constraint.

3. **Extend the upload endpoint compatibly**
   - Preserve the existing synchronous behavior by default.
   - Introduce asynchronous/resumable processing through an explicit option or a new compatible endpoint.
   - Return an import ID and progress location for background imports.
   - Avoid changing existing response fields or status codes relied upon by clients.

4. **Make the background job resumable and idempotent**
   - Process rows in bounded batches.
   - Commit accepted records and the corresponding checkpoint atomically.
   - Upsert or claim rows using the stable idempotency key.
   - Make retries safe after failures occurring before, during, or after a batch commit.
   - Persist validation failures without aborting otherwise valid rows.

5. **Add progress and error reporting**
   - Expose import status and counters through a polling endpoint or the application’s existing event mechanism.
   - Update the UI with progress, completion state, retryable failures, and invalid-row download/details.
   - Ensure refreshes and reconnects recover progress from server state.

6. **Verify the complete behavior**
   - Existing synchronous-path regression tests.
   - Mixed valid/invalid CSV test.
   - Retry the same batch and prove accepted-record counts do not increase.
   - Simulate interruption between batches and prove processing resumes at the checkpoint.
   - Simulate failure around commit boundaries.
   - Verify concurrent duplicate job delivery.
   - Verify UI states: queued, processing, partial rejection, completed, failed, and resumed.
   - Run the relevant test suites and capture concrete output.

### Design guardrails

- The database, rather than worker memory, will provide the final duplicate-prevention guarantee.
- Checkpoints will advance only for durably handled rows.
- Row errors will not expose sensitive input unnecessarily.
- CSV files and error artifacts will follow existing retention and access-control rules.
- Schema changes, if needed, will be additive and reversible.

### Expected proof at handoff

The completed handoff should include:

- Files and schema changed, with the reason for each.
- Before/after API compatibility evidence.
- Test output covering invalid rows, retries, interruption, and duplicate prevention.
- A concise manual UI verification result.
- Any remaining uncertainty and a rollback path for additive schema or feature-flag changes.

Next action: inspect the repository and convert this plan into file-specific tasks and acceptance tests before implementation.
