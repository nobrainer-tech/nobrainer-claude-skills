## Case A

v2.4.0 is deployed to 8 of 10 regions at 09:18 UTC; two remain paused at 472 ms and 489 ms after latency exceeded 450 ms. The cause is unconfirmed. Mei owns the investigation and will post a new trace by 10:00 UTC. Resume only after both regions stay below 400 ms for 20 continuous minutes, then rerun `./verify-regions.sh` for all ten.

## Case B

Invoice INV-884 is blocked because the purchase-order number is missing. Keep it in draft; Ola will add the number and resubmit it today.

## Case C

1. Create a backup with `./backup.sh`.
2. Only after it exits successfully, run `./migrate.sh --dry-run`.
3. Never run `./migrate.sh --apply` without written owner approval.

A successful dry run proves only that local validation passed; it does not prove the production migration will succeed.

## Case D

In a 24-ticket internal sample, the median first-response time was 4m08s.

## Case E

Proszę o usunięcie `leads-2026-08.csv` do 31 sierpnia 2026 r., godz. 16:00 CEST, oraz o pisemne potwierdzenie po usunięciu. Plik zawiera dane osobowe i nie może być dołączony do wiadomości.
