# Frozen writing release holdout — v1.2.0

This prompt was frozen after the development probe and first final holdout, and
before its first run. Read only `skills/nobrainer-writing/SKILL.md`. Do not
inspect tests, release notes, research references or other evaluation artifacts.
Do not edit files, browse or invent missing facts.

Act as NoBrainer Writing. Treat every case as independent. Return exactly five
sections named `## Case A` through `## Case E`. Under each heading return only
the finished prose requested by that case, with no process commentary.

## Case A — executive status

Compress the source into an English owner update of at most 65 words. Lead with
the current outcome. Preserve every material version, number, time, uncertainty,
owner, action and close condition. Do not call the rollout complete.

Source:

> We deployed v2.4.0 to 8 of 10 regions at 09:18 UTC. The remaining two regions
> are paused because latency exceeded 450 ms; the current readings are 472 ms
> and 489 ms. The cause has not been confirmed. Mei owns the investigation and
> will post a new trace by 10:00 UTC. Resume only after both regions stay below
> 400 ms for 20 continuous minutes, then rerun `./verify-regions.sh` for all ten.

## Case B — already sufficient

Rewrite only if doing so is materially clearer. Do not expand the message or
add a greeting, conclusion or explanation.

Source:

> Invoice INV-884 is blocked because the purchase-order number is missing. Keep
> it in draft; Ola will add the number and resubmit it today.

## Case C — exact operational note

Write the shortest safe runbook block. Preserve the exact order, commands,
prohibition and evidence boundary.

Source:

> First create a backup with `./backup.sh`. Only after it exits successfully,
> run `./migrate.sh --dry-run`. Never run `./migrate.sh --apply` without written
> owner approval. A successful dry run proves only that local validation passed;
> it does not prove the production migration will succeed.

## Case D — anti-hype product copy

Rewrite as one natural sentence for a changelog. The only evidence is the
supplied observation. Do not add causation, customer sentiment, comparison,
urgency, broad representativeness or an invented voice.

Source:

> Our groundbreaking AI revolutionizes every support workflow with effortless,
> world-class speed. In a 24-ticket internal sample, median first-response time
> was 4m08s.

## Case E — privacy-preserving request

Draft a concise Polish message with one request. Preserve the exact file name,
deadline, confirmation requirement, personal-data status and no-attachment
condition. Add no legal claim.

Established facts:

- Poproś o usunięcie `leads-2026-08.csv` do 31 sierpnia 2026, 16:00 CEST.
- Poproś o pisemne potwierdzenie po usunięciu.
- Plik zawiera dane osobowe i nie może być dołączony do wiadomości.
