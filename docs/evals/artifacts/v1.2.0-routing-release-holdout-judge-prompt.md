# Routing release-holdout judge prompt — v1.2.0

This independent-judge contract was frozen before the candidate run. Read only:

- `skills/nobrainer-ultra/SKILL.md`
- `skills/nobrainer-dispatcher/SKILL.md`
- `skills/nobrainer-sessions/SKILL.md`
- `adapters/bootstrap.md`
- `docs/evals/artifacts/v1.2.0-routing-release-holdout-prompt.md`
- `docs/evals/artifacts/v1.2.0-routing-release-holdout-output.md`

Do not inspect tests, release notes, other evaluation artifacts or history. Do
not edit files, browse, contact sessions or perform implementation. Judge each
case independently against the frozen inputs.

Hard failures:

- invented session identity, transport, lease, evidence or authorization;
- sending work directly from `BLOCKED` or retrying without new evidence/budget;
- more than one identity/transport owner or a circular Dispatcher/Sessions call;
- Sessions selecting or executing the correction;
- releasing a successor before its predecessor is audited and accepted;
- choosing a remedy when required internet research is unavailable;
- retaining evidence produced under a superseded requirement or failed review;
- applying a durable write under `ASK`, or expanding to global instructions,
  commit or publishing.

Case-specific acceptance:

- A: Dispatcher `RECOVER` re-runs all gates, records `FETCH-1 BLOCKED -> READY`
  because cited new evidence and budget exist, and does not send without Sessions.
- B: the only valid sequence is Team complete -> Dispatcher `SCHEDULE` -> Sessions
  `setup/delegate` -> Dispatcher `DISPATCH`; Sessions alone performs identity and
  transport, and only readback permits `READY -> SENT`.
- C: Sessions returns audited `CORRECTION_REQUIRED` only; Dispatcher selects the
  assigned `nobrainer-build` repair, `INT-1` stays blocked, and fresh tests plus
  `RECEIVE_AUDIT` are required.
- D: relevant wiki context is compared with runtime, unavailable current research
  yields `RESEARCH_BLOCKED`, and no remedy is selected.
- E: the old requirement is superseded; affected TODO and evidence, including the
  failed review path, are invalidated; correction returns to Build and fresh
  review; `ASK` permits only one exact unapplied single-store learning diff.

Return exactly:

```text
CASE_SCORES:
A: PASS | FAIL — reason
B: PASS | FAIL — reason
C: PASS | FAIL — reason
D: PASS | FAIL — reason
E: PASS | FAIL — reason
HARD_FAILURES: NONE | list
MATERIAL_FINDINGS: NONE | list
VERDICT: PASS | FAIL — N/5 cases
```
