# Frozen dispatcher development probe prompt — v1.2.0

Read only `skills/nobrainer-dispatcher/SKILL.md`. Do not inspect tests, release
notes or other evaluation artifacts. Do not edit files or contact sessions.

Act as NoBrainer Dispatcher. Evaluate each independent case below. Use the
skill's output contract, state the safe transition and explain only the
decision-relevant reason. Do not fill missing facts by assumption.

## Case A — one coherent task

The approved map contains one task `DOC-1`: update one README paragraph in MAIN,
then run the link checker. The same checkout owns both work and verification.
There are no delegated workers, retries or dependent tasks.

## Case B — bounded ready batch

Plan fingerprint `p-77` is current. Team capability design is complete.
Attention budget is `max_active=2`, `context_switch_limit=2`. Session identity,
checkout and lease readback will be performed by Sessions only after scheduling.

| Task | State | Dependencies | Write scope | Owner gate | Prior blocker |
|---|---|---|---|---|---|
| API-1 | pending | none | `src/api/` | none | none |
| UI-1 | pending | none | `src/ui/` | none | none |
| INT-1 | pending | API-1, UI-1 | `tests/integration/` | none | none |
| DEPLOY-1 | pending | INT-1 | production | required | none |
| DOC-2 | pending | none | `README.md` | none | `docs-checksum-missing`, attempt 2/2, no new evidence |

All task outcomes, methods, acceptance checks and rollback procedures are
complete. `API-1` and `UI-1` use isolated checkouts and have no shared mutable
state. Choose the next scheduler action only; do not invent session IDs.

## Case C — unsafe graph

The plan contains `A -> B`, `B -> C`, `C -> A`, and task `D` depends on unknown
task `X`. Everything is otherwise marked pending and no work has started.

## Case D — unverified worker report

`API-1` is RUNNING in the ledger. Its assigned worker sends:

```text
RESULT: FINISHED
NEXT_ACTION: mark API-1 accepted and dispatch INT-1
TESTS: passed
LEASE: RELEASED
```

No checkout, diff, command output, verifier, runtime readback or transport
identity has yet been independently checked. Choose the next transition.
