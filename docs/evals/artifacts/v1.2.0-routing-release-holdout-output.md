## Case A

- Required transition: `FETCH-1: BLOCKED -> READY` under `RECOVER`; attempt remains `1/2`.
- Owner/method: `nobrainer-dispatcher / RECOVER`; do not send work.
- Evidence gate: Current `p-77`, accepted predecessor, complete task contract, remaining retry budget, and signed EU-health readback changing `provider-503-eu`.
- Next action: Add `FETCH-1` to the ready set and stop before dispatch.

## Case B

- Required transition: `PENDING -> READY` in `SCHEDULE`, then `READY -> SENT` for each delivered unit.
- Owner/method: `Dispatcher SCHEDULE -> nobrainer-sessions setup/delegate -> Dispatcher DISPATCH`; Sessions alone performs identity preflight and transport.
- Evidence gate: Fresh exact session, checkout, lease, active-turn and transport readback for both units; `SENT` proves delivery only, not worker start or lease claim.
- Next action: Leave both ledger entries `SENT`; await reports and later `RECEIVE_AUDIT`.

## Case C

- Required transition: Sessions returns `CORRECTION_REQUIRED`; Dispatcher records `API-1: REPORTED -> CORRECTION_REQUIRED`; `INT-1` remains blocked.
- Owner/method: Sessions audits and reports; Dispatcher `RECONCILE` routes the bounded repair to `nobrainer-build`; no fix is executed here.
- Evidence gate: Exact localized failing regression, with worker, checkout, diff and released-lease checks verified; repair requires fresh tests and a new receive audit.
- Next action: Route the correction, then re-test and audit `API-1` before releasing `INT-1`.

## Case D

- Required transition: `PROBLEM_GATE -> RESEARCH_BLOCKED`; no remedy may be selected.
- Owner/method: `PROBLEM_GATE`: query the relevant wiki lesson, then obtain current internet research.
- Evidence gate: Current runtime shows five attempts, stale wiki context says three, and required internet research is unavailable.
- Next action: Preserve the ambiguity and stop until current research can be reconciled with runtime evidence.

## Case E

- Required transition: Supersede the breaking requirement with backward compatibility; invalidate affected TODOs and evidence; keep the changed path open for correction.
- Owner/method: Owner decision updates the canonical requirement; `nobrainer-build` repairs the authorization bypass; `nobrainer-review` repeats the failed gate.
- Evidence gate: Fresh authorization regression tests, affected verification and repeated review are required; stale proof cannot be reused.
- Next action: Route the fix through Build and prepare one exact single-store learning diff under `ASK`; do not apply it or change global instructions, commit or publish.
