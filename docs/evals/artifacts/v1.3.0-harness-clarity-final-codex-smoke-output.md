I’ll add an optional `--json` mode while preserving the default output byte-for-byte.

- Scope: only `src/report.py` and `tests/test_report.py`.
- Proof: one regression test for unchanged text output and one test for the JSON shape.
- Excluded: documentation, dependencies, and unrelated refactoring.

Progress

- [x] Scope and acceptance are clear
- [>] Inspecting the current CLI output path and existing tests
- [ ] Implement and verify

Next: inspect `src/report.py` and `tests/test_report.py`, then make the smallest compatible change.
