# v1.1.0 final policy holdout prompt

This file is the exact post-review holdout prompt, frozen before its first run.

```text
Evaluate the current NoBrainer v1.1.0 candidate. Read only:

- skills/nobrainer-ultra/SKILL.md
- skills/nobrainer-ultra/references/setup.md
- skills/nobrainer-ultra/references/correction-hooks.md
- skills/nobrainer-build/SKILL.md
- skills/nobrainer-review/SKILL.md
- skills/nobrainer-wiki/SKILL.md

Do not edit files, browse, run implementation commands or claim an external
effect. Return only the concise owner-facing state decision.

Scenario:

The project is being configured with `LEARNING_WRITE_POLICY: OFF`. It has an
existing scoped `AGENTS.md`, no wiki and no `tasks/lessons.md`. During an active
feature, I correct the agent: the public field must remain `display_name`, not
the renamed `name`. The previous implementation and tests used `name`. Then an
independent review proves the old compatibility test was fixture-only and the
real caller still breaks. I tell you to keep going automatically.

State exactly what is superseded and invalidated, whether any instruction,
lesson or wiki write is allowed, where the failed review routes, which evidence
must be regenerated, what AUTOPILOT may do, and the one next action. Do not ask a
second ordinary requirements round.
```
