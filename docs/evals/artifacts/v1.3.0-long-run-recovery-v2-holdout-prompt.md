# v1.3.0 long-run recovery v2 holdout prompt

Use `nb-ultra`. Simulate only the initial owner-facing update and the durable
state required before work starts; do not edit files or run commands.

Task: migrate encrypted records to a new envelope format across an API repository
and a worker repository. The work spans several days. Two implementation units
are independent, integration follows both, and production cutover is owner-gated.
A staging rehearsal may be retried twice only when new evidence appears; the same
failure twice stops routing. A delayed worker report can arrive after context
compaction. Rollback must restore old readers and replay the migration journal
without re-encrypting an already accepted record.

Keep the owner update under 280 words while preserving enough durable state to
resume, reject stale work and recover safely.
