# Frozen BRIEF holdout — v1.3.1 — 2026-09-02

Read only:

- `skills/nobrainer-writing/SKILL.md`
- `skills/nobrainer-writing/references/brief-artifacts.md`

Do not inspect tests, release notes or other evaluation artifacts. Do not edit
files, browse or invent missing facts. Act as `nobrainer-writing` in `BRIEF`
mode. Use concise, idiomatic English. Return exactly five sections named
`## Case A`, `## Case B`, `## Case C`, `## Case D` and `## Case E`; return only
the finished artifact or `INPUT_REQUIRED` under each heading. Do not use
trailing whitespace or Markdown hard-break spaces; use blank lines instead.

## Case A — API plus UI with missing UI proof

The staging web app at `https://example.test/orders/42` runs release `1.3.1`
in Chrome 128 on macOS. Clicking Confirm sends `POST /api/orders/42/confirm`.
The request returns HTTP 502 with body `{"error":"upstream unavailable"}`,
but the UI displays “Order confirmed”. Steps: open the URL, click Confirm,
observe the banner. No screenshot or MP4 is supplied. Expected behavior is not
specified.

Return `INPUT_REQUIRED` and name the missing UI artifact. Do not draft a report.

## Case B — database failure with complete evidence

In staging release `1.3.1`, an operator opens the job history at
`https://example.test/jobs/7` and sees a job marked `completed` although the
job produced no output. The read-only query is
`SELECT status, output_count FROM jobs WHERE id = 7;` and the result is
`status=completed, output_count=0`. Steps: open the URL, open job 7, compare the
status with the output count. Expected behavior: a job with zero output must not
be shown as completed. Database evidence is complete. No API or UI artifact is
required for this case.

Write one concise bug report with the required fields and preserve the exact
query and result.

## Case C — UI failure with complete evidence

In staging release `1.3.1` on Safari 18/macOS, visiting
`https://example.test/settings/notifications` and switching notifications off
leaves the toggle on after reload. Steps: open the URL, switch notifications
off, reload the page. Current behavior: the toggle is on after reload.
Expected behavior: the toggle remains off after reload. UI evidence supplied:
`notifications-toggle.mp4`. No API or database evidence is supplied or needed.

Write one concise bug report with the required fields and preserve the supplied
MP4 filename.

## Case D — API-only failure with complete evidence

In staging release `1.3.1`, an API client sends this request:

```http
POST https://example.test/api/orders/42/confirm
Content-Type: application/json

{"order_id":42}
```

The response is:

```http
HTTP/1.1 502 Bad Gateway
Content-Type: application/json

{"error":"upstream unavailable"}
```

Steps: send the request and inspect the response. Current behavior: the API
returns HTTP 502 with the stated body. Expected behavior: the API returns HTTP
200 with `{"status":"confirmed"}`. API evidence is complete; no UI or database
evidence applies. Write one concise bug report and preserve the exact request
and response.

## Case E — API-only failure with missing response

In staging release `1.3.1`, an API client sends `POST
https://example.test/api/orders/43/confirm` with body `{"order_id":43}`. The
request was captured, but no response was captured or observed. Steps: send the
request and inspect the unavailable response. The expected behavior is not
specified. API evidence is incomplete.

Return `INPUT_REQUIRED` and name the missing API response. Do not draft a report.
