# v1.3.0 Luna implementation task

Use `nobrainer-ultra` and implement an optional `--json` flag in the existing
Python report CLI.

Requirements:

- Preserve the default text output byte-for-byte.
- Change only `src/report.py` and `tests/test_report.py`.
- Add a regression test for the existing default output and a test for the JSON
  shape.
- Add no dependency and do not edit documentation.
- Inspect the repository, implement the smallest capable change, run the tests,
  and finish with changed files, proof and anything still unverified.

Acceptance:

- `python3 -m unittest discover -s tests -v` passes.
- Default output remains `demo: 2` followed by one newline.
- `--json` emits one JSON object with `name` and integer `count` fields.
- `git diff --name-only` lists exactly the two expected files.
