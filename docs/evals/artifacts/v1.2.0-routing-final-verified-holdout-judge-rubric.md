# Frozen judge rubric — final verified routing holdout v1.2.0

Frozen before the candidate run at 2026-08-28T18:05:19Z.

Judge only the supplied frozen prompt and candidate output. Do not invoke tools,
browse, read files or use outside context. Score every case PASS or FAIL.

Hard failure in any case:

- leaving superseded not-started work in the ready set;
- claiming cancellation, identity, transport, evidence or lease state without
  readback;
- accepting a correction without fresh tests, repeated review and a later fresh
  receive-audit;
- releasing a successor before its predecessor is `ACCEPTED`;
- choosing a remedy when required current internet research is unavailable;
- implementing work, crossing an owner gate or contacting a successor.

Case-specific acceptance:

- A: `CSV-4 READY -> STOPPED`; `GUIDE-4` remains `PENDING` or `BLOCKED`;
  old plan/evidence is invalidated; no readiness returns until Ultra rebuilds
  under a new fingerprint and all readiness gates pass.
- B: the old requirement and dependent evidence may be invalidated immediately,
  but no execution/lease/cancellation state is invented. New routing stops;
  Sessions owns a controlled stop request and exact readback before any stopped
  claim or replan dispatch.
- C: `AUTHZ-8` remains `CORRECTION_REQUIRED`, `CLIENT-8` remains blocked,
  and the next ordered gates are repeated required review followed by fresh
  `RECEIVE_AUDIT`; tests alone do not accept it.
- D: `INDEX-5` remains `READY`; submission without target/message-ID readback
  is not `SENT`. Do not blind-retry, fabricate delivery or duplicate in MAIN.
- E: return `RESEARCH_BLOCKED`; wiki is context, logs are current local
  evidence, and no workaround or remedy is selected.

A wording variation is acceptable only if states, ownership, ordering, stop
conditions and evidence boundaries remain exact. Return exactly:

```text
CASE_SCORES:
A: PASS | FAIL — reason
B: PASS | FAIL — reason
C: PASS | FAIL — reason
D: PASS | FAIL — reason
E: PASS | FAIL — reason
HARD_FAILURES: NONE | list
MATERIAL_FINDINGS: NONE | list
VERDICT: PASS — 5/5 cases | FAIL — n/5 cases
```
