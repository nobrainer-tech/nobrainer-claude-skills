## Case A

28 August 2026: 47 of 52 accounts migrated. Five remain blocked because Acme’s SSO metadata is missing. `python3 scripts/verify_migration.py` passed for all 47 migrated accounts; production login has not been tested for the five blocked accounts. Anna needs to send the metadata by 14:00 CEST, then Jan will rerun the verifier. Do not call the migration complete until all 52 production login checks pass.

## Case B

PR #42 is blocked: the Linux job fails on Python 3.14 because `distutils` is unavailable. Keep the PR open; Marta will replace the dependency and rerun CI.

## Case C

In July, the platform processed 12,480 invoices with a median review time of 3m12s.

## Case D

Backups are encrypted at rest and retained for 30 days. Deleting a workspace starts deletion, but legal holds may delay final removal; deletion is not immediate. Admins should export required records first.

## Case E

```python
# The third retry is the externally agreed budget; further retries require new evidence.
```
