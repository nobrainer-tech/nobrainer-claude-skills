# Frozen writing final holdout — v1.2.0

This prompt was frozen after the development probe and before its first run.
Read only `skills/nobrainer-writing/SKILL.md`. Do not inspect tests, release
notes, research references or other evaluation artifacts. Do not edit files,
browse or invent missing facts.

Act as NoBrainer Writing. Treat every case as independent. Return exactly five
sections named `## Case A` through `## Case E`. Under each heading return only
the finished prose requested by that case, with no process commentary.

## Case A — Polish incident update

Turn the source into a concise Polish status for the project owner. Preserve
every material version, time, measurement, uncertainty, owner and stop
condition. Do not say the incident is resolved.

Source:

> Chciałem tylko przekazać najnowszy update. O 12:07 wdrożyliśmy rollback z
> v3.8.1 do v3.8.0. Odsetek błędów spadł z 18% do 2%, ale przyczyna źródłowa
> nadal nie jest potwierdzona. Piotr prowadzi RCA. Monitorujemy system do 13:00;
> dopiero jeśli poziom błędów pozostanie poniżej 3% przez cały ten okres,
> podejmiemy decyzję o zamknięciu incydentu.

## Case B — one clear email request

Draft a short email to the vendor. Include one clear request and deadline.
Preserve the privacy constraint; do not add legal claims or say the file is
attached.

Established facts:

- Ask the vendor to delete `customer-export-2026-08-28.csv` by 2 September 2026.
- Ask for written confirmation after deletion.
- The file contains personal data and must not be attached to the email.

## Case C — safe installation instructions

Rewrite the source as the shortest safe instruction block. Preserve command
order, the prohibited command and the evidence boundary.

Source:

> You need Node 22 before doing anything else. Then run `npm ci`, and only when
> that finishes successfully run `npm test`. Never run `npm audit fix --force`
> in this repository. An exit code of 0 proves the local test suite passed; it
> does not prove production is healthy.

## Case D — nuanced argument

Compress the source into one coherent paragraph. Keep the complete thesis,
both limiting conditions and the recommendation. Do not turn it into slogans.

Source:

> Multiple agents can reduce elapsed time when tasks are genuinely independent
> and each result can be checked on its own. That benefit disappears when the
> agents contend for the same mutable files or when coordination and review cost
> more than the work itself. A single coherent change should therefore stay
> with one agent, while parallel sessions should be reserved for bounded work
> with disjoint ownership and explicit acceptance checks.

## Case E — evidence-led social copy

Write one natural sentence suitable for a product update. Preserve both
measurements and the fact that they describe the beta, not all users. Add no
testimonial, comparison, causation, urgency or invented voice.

Source:

> In the 40-user beta, 31 participants completed onboarding without support and
> the median completion time was 6 minutes.
