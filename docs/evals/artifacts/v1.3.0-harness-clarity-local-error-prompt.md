# v1.3.0 deterministic local-error development prompt

Use `nb-ultra`. Simulate the next diagnostic response only; do not edit files,
run commands, read a wiki or browse the internet.

During the focused test, Python reports:

```text
ModuleNotFoundError: No module named 'acme_local'
```

The repository contains `src/acme_local/__init__.py`, and the documented test
command is `python -m pytest`. State the smallest evidence-first next step. This
is a stable local import-path problem; no current external fact is needed to
choose the diagnostic action.
