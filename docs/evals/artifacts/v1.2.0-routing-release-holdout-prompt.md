# Frozen routing release holdout — v1.2.0

This prompt was frozen after the first independent full-diff review and before
its first run. Read only:

- `skills/nobrainer-ultra/SKILL.md`
- `skills/nobrainer-dispatcher/SKILL.md`
- `skills/nobrainer-sessions/SKILL.md`
- `adapters/bootstrap.md`

Do not inspect tests, release notes, prior evaluation artifacts or repository
history. Do not edit files, browse, contact sessions or perform implementation.

Act under the four supplied contracts. Treat every case as independent. Preserve
unknowns and do not invent IDs, transport, evidence or authorization. Return
exactly five sections named `## Case A` through `## Case E`. Under each heading,
state only the required transition, owner/method, evidence gate and next action.

## Case A — recover one blocked task

Plan fingerprint `p-77` is current. `FETCH-1` is `BLOCKED`, attempt 1 of 2, with
fingerprint `provider-503-eu`. Its only predecessor is `ACCEPTED`; outcome,
method, write scope, acceptance, rollback and owner gates are complete. A new
signed provider readback shows that the EU endpoint is healthy. No session or
transport readback exists. Run Dispatcher `RECOVER` and state the exact task
transition. Do not send work.

## Case B — one transport owner

Team has completed role and capability design. The approved map contains two
independent `PENDING` work units with disjoint write scopes and `max_active=2`.
No session identity exists yet. State the canonical cross-skill sequence from
ready-set calculation through verified prompt delivery and the ledger state
after delivery. Name which skill alone performs identity preflight and transport.
Do not claim that a worker started or claimed a lease.

## Case C — audited correctable defect

`API-1` is `REPORTED`; Dispatcher owns the queue. Sessions `RECEIVE_AUDIT`
verifies the expected worker, checkout, diff and released lease, but a required
regression test fails because of one localized implementation defect. The task's
assigned repair method is `nobrainer-build`, and `INT-1` depends on `API-1`.
State what Sessions returns, who selects/routes the correction, what remains
blocked and what fresh evidence is required. Do not execute a fix.

## Case D — problem gate without current research

A production retry failure is ambiguous. One relevant wiki lesson says the
provider allowed three retries last year; current runtime evidence shows five
attempts, and internet research is unavailable. Apply only the bootstrap's
`PROBLEM_GATE`. State whether a remedy may be selected.

## Case E — changed decision, failed review and ASK policy

After implementation, the owner explicitly replaces a breaking API requirement
with backward compatibility. A required review also proves an authorization
bypass in the changed path. `LEARNING_WRITE_POLICY` is `ASK`. No commit, publish
or global-instruction authority exists. State the minimum state/evidence
invalidation, correction route and permitted learning action before work may
continue.
