# Version 1.8.0 — adaptive session restart and portable refinements

This source record covers the v1.8.0 candidate prepared on 2026-09-06 from
`origin/main` at `50de71c0c32f500da181f214062cc405b904a506`. It is a handoff for
owner audit. It does not claim a merge, tag, GitHub release, marketplace
distribution, client loading or clean-session runtime behavior.

## Scope

- The existing `nobrainer-sessions` portfolio now ships the adaptive
  `session-restart` protocol and its deterministic Python decision helper. The
  protocol requires a durable checkpoint, exact identity, verified takeover and
  controlled retirement before the source session is archived. It does not add a
  sixteenth skill, watcher daemon, scheduler, global hook or native session API.
- The source-backed OpenAI Codex refinements already present in the base are
  included: bound restart context fragments, preserve reusable input order and
  bytes where a host supports reuse, bind review monitoring to the exact head,
  exclude pending draft feedback, keep retries finite and diagnosed, and select
  only review perspectives justified by the changed boundary. The adoption map
  remains [source-contract evidence](../reviews/2026-09-05-openai-codex-skills.md),
  not model-quality, token-cost or provider-runtime evidence.
- Active package and adapter metadata is consistently `1.8.0` across the npm
  package, lockfile, root plugin, Claude, Codex, Cursor, Kimi, Gemini and
  marketplace manifests. The fifteen canonical skills, package/plugin IDs and
  client-neutral adapter paths remain unchanged.
- The active README, release notes, compatibility statement and session-restart
  guide now describe the v1.8.0 source and keep the published v1.7.1 source as
  the rollback anchor. Dated v1.7.1 release, publication, review and evaluation
  records retain their original bytes.

## Verification boundary

The release-preparation checks cover portable source and repository behavior:

- `python3 scripts/validate_skills.py` and
  `python3 scripts/validate_skills.py --suite` validate the active portfolio,
  manifests, links and public-clean rules.
- `python3 -m unittest discover -s tests -q` covers the deterministic adapter,
  installer, security, lifecycle, restart and suite gates.
- The restart helper is invoked through its real Python CLI with a normalized
  observation and must return its deterministic `CREATE_FRESH` decision; the
  helper never creates, sends, archives or locks a session.
- `git diff --check`, the active-manifest equality readback and the staged
  gitleaks policy scan cover the handoff bytes before publication.

The fresh run returned `OK` from both validators, `154 tests ... OK` from the
full unittest suite, `CREATE_FRESH` from the helper smoke input and no leaks from
the staged gitleaks scan.

These checks establish source and repository proof only. No client was installed
or exercised for this candidate, and no claim is made about native transport,
all-client restart support, model quality, token savings, billing or marketplace
availability. The owner must perform the merge, tag, release and any external
readback separately.

## Rollback

The rollback anchor is the published `v1.7.1` tag at commit
`a5b2dcccae3aeb8e64f19f121f98f74f59719d82`. Pin that exact commit (or the tag
after verifying it resolves to the same commit), restore the v1.7.1 active
metadata and verify the source with the checks belonging to that tree. Keep the
v1.8.0 checkout and any audit evidence until the rollback is accepted; do not
delete unrelated installations or rewrite historical release records.
