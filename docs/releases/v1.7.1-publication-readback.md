# v1.7.1 publication readback — 2026-09-05

NoBrainer Tech Flow v1.7.1 is published at
<https://github.com/nobrainer-tech/nobrainer-tech-flow/releases/tag/v1.7.1>.
The release tag resolves to `a5b2dcccae3aeb8e64f19f121f98f74f59719d82`.
The old and new GitHub repository addresses resolve to the same release commit.

## Product and installation

- Public product: NoBrainer Tech Flow, “From task to done.”
- Canonical site: <https://nobrainer.tech/flow/>.
- The 15 module names, `nb-ultra`, package/plugin installation identities and
  bootstrap marker remain compatible. This is a branding release; the bounded
  runner and protected runtime files retain their v1.7.0 behavior.
- Downloaded public v1.7.1 archive: suite validator passed; isolated copy
  installation passed; the installed bounded runner returned `completed`.
- Linux and macOS post-merge checks passed:
  <https://github.com/nobrainer-tech/nobrainer-tech-flow/actions/runs/33951044769>.

## Website and coverage

The collected Trending snapshot covers 435 discoveries across repository and
featured-developer views with daily, weekly and monthly ranges and selected
language categories. Reconciled metadata screening and targeted pinned source
review produced 40 canonical comparison pages. This does not mean 435 runtime
audits or exhaustive coverage of all languages and future Trending results.

The initial Flow deployment matched all 58 staged public files. Nine redirect
checks covered root, deep comparison with query and asset paths on the two
subdomains and former `/skills/` prefix. Both subdomains have working HTTPS.
The pre-existing server-only Runpane legacy page remains available.

The owner-requested comparison clarity update is also published. Website PR 8
merged as `61bbda65f968368a59616f4edbbee40e6ef0d001`: all 40 source pages
now explain concrete source strengths, both task routes, additional Flow value
and practical fit, with details below. All 56 current Flow files matched the
public readback; nine redirect cases passed again. The site validator, ten
contract tests, ten desktop/mobile route checks and live mobile ECC readback
passed. The library version stays v1.7.1.

Verification digest and scope: [publication artifact](artifacts/v1.7.1-publication.json).

## Social and weekly research

Authenticated readback confirmed exactly seven existing scheduled X posts on
@nobrainer_tech, September 5–11 at 18:00 Europe/Warsaw, with the updated brand,
current version where mentioned and `/flow` links. Their dates were preserved;
no duplicate posts were created. The saved public bio identifies the account as
creator of NoBrainer Tech Flow. Historical published posts were not rewritten.

The weekly ecosystem task is active for Friday 09:00 Europe/Warsaw, next run
September 11, using Sol low. It reads the current published release and existing
queue, limits deeper source reads, and prepares drafts without auto-publishing.

## Proof limits and rollback

No new controlled model-behavior benchmark was run for this branding release.
The optional runner bounds one process group by time and captured output; it is
not an account quota, token billing limiter, sandbox or persistent agent engine.

Previous release tags remain intact. Website preimages and checksum manifests
were retained before each transfer; deployment used checksum comparison and no
remote deletion. Existing domains, installation identifiers and dated historical
evidence remain available for rollback and compatibility.
